{{define "footer" -}}
    <script>
    document.querySelectorAll('.listing-link').forEach(function (link) {
        link.addEventListener('click', function (event) {
            event.target.closest('.listing').classList.add('listing-visited');
        });
    });
    </script>
</body>
</html>
{{end}}